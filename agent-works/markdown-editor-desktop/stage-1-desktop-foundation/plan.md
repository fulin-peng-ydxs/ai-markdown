# Plainroot 第一阶段开发计划

> 当前执行分支：`codex/plainroot-stage-1`
>
> 当前阶段：阶段 1——原生桌面壳、本地授权、文件模型、安全写入底座和窗口创建/目录决策壳
>
> 计划状态：进行中（T1～T7 已完成，T8 待开始）
>
> 需求编号规则：完全沿用 `requirement.md` 的 R1～R34，不新增、重排或改变 R 编号含义。

## 1. 需求来源与目标

- 需求文档：`agent-works/markdown-editor-desktop/requirement.md`。
- 上游闭环：`agent-works/markdown-editor-desktop/requirement-closure.md`。
- 页面原型：
  - `agent-works/markdown-editor-desktop/prototypes/markdown-workbench.html`；
  - `agent-works/markdown-editor-desktop/prototypes/workspace-launcher.html`；
  - `agent-works/markdown-editor-desktop/prototypes/theme-preset-studio.html`。
- 功能目标：在真实 macOS/Windows 桌面壳内打开用户授权的目录或 Markdown 文件，建立受根目录约束的文件模型，完成文件树、文件与文件夹管理、安全写入原语、最近工作区、原生窗口创建、同目录窗口聚焦和目录打开决策壳，为后续文档模型、页签和保存状态机提供可测试底座。
- 第一阶段边界：
  - 完成 R1 的桌面壳、本地授权和离线底座子集；
  - 完成 R2 的扫描、读取、文件树、增删改移、回收站、外部变化监听等文件系统子集；
  - 完成 R5 的安全写入、文件修订信息和失败不破坏原文件子集，不宣称完成自动保存、恢复快照或外部冲突状态机；
  - 完成 R14 的窗口创建、同目录聚焦、目录决策、最近记录和根工作区会话子集，不宣称完成多页签保存检查与完整会话恢复；
  - P2 启动页和 P1 工作台壳进入第一阶段；P1 编辑、页签、完整布局、阅读、搜索及 P3 主题工作室由后续独立阶段计划承接。
- 本次不做：R3～R4、R6～R13、R15、R30、R32 的完整产品能力；R11、R31 仅承接 P1/P2 壳所需子集；R16、R17、R24～R26、R33、R34 后续建议；R18～R23、R27～R29 暂不实现项。阶段外需求仅保留范围状态、验收依据和另立计划约束，不在第一阶段预占后续任务编号。
- 计划规则例外：`auto-plan-dev` 原则上要求每个必须实现项至少对应一个任务；本文件是用户明确要求的第一阶段专属计划，且用户明确要求不放置后续阶段占位任务，因此阶段外必须实现项有意识地采用“跳过 + 未覆盖 + 另立计划义务”登记，任务编号延至对应阶段计划分配。这是阶段化取舍，不代表取消产品义务；进入对应阶段前必须消除该项未覆盖状态。
- 计划输出目录：`agent-works/markdown-editor-desktop/stage-1-desktop-foundation/`。

## 2. 当前项目依据

### 2.1 仓库与工具链事实

- 当前仓库已完成 T1 工程基线、T2 领域契约、T3 原生窗口壳、T4 版本化本地状态、T5 系统选择/授权管线、T6 只读文件底座和 T7 新建/重命名/移动底座：具备可构建的 Tauri/React 空壳、许可证策略测试、Rust/TypeScript 共享模型、授权根与相对路径安全校验、原生窗口/菜单、原子 JSON 状态仓储、系统选择器命令、后台扫描/读取以及不覆盖目标的工作区变更命令与渐进树状态；尚无删除/回收站、外部监听、工作区页面、完整窗口打开协调、数据库、产品 E2E 或 CI 基线。
- 本机 NVM 已安装 Node `v24.11.1`、npm `11.6.2`、Corepack `0.34.2` 和 pnpm `11.5.1`；当前默认 Node 仍为 18，因此项目必须用 `.nvmrc` 和 `packageManager` 固定工具链，所有本地命令先执行 `nvm use`。
- 本机已安装 macOS Command Line Tools、Apple Clang 16，以及由 T1 安装并锁定的 Rust/Cargo 1.97.1、rustfmt 与 clippy；完整 Xcode 未安装且移动端不在本阶段范围。
- Windows 实机无法由用户提供。第一阶段使用 GitHub Actions `windows-latest` 完成真实 Windows 编译、Rust/前端测试和 WebdriverIO 桌面 E2E；原生文件选择器、系统回收站、文件管理器定位等不适合稳定无人值守验证的交互，保留为最终跨平台/发布前 Windows 人工验收，不虚假标记为已通过。
- 技术依据：Tauri 2 官方要求 Rust 和 LTS Node，Vite 8 支持 Node 20.19+ 或 22.12+；Tauri 官方测试方案支持 mock runtime、WebdriverIO Tauri 服务和 CI runner。依赖锁定时记录精确版本、许可证和安全审计结果。

### 2.2 页面与设计依据

- 前端视觉、布局和交互的权威规范是仓库根目录 `DESIGN.md`；P1/P2 的实现任务 T12～T14 必须逐项执行 `agent-works/markdown-editor-desktop/page-development-workflow.md`，原型只提供区域、状态和交互证据，不能覆盖正式规范。
- P1 布局事实源是 `prototypes/markdown-workbench.html`：原生窗口层、页签层、文档工具层、左文件树、中部画布、右大纲、状态栏和目录决策对话框。
- P2 布局事实源是 `prototypes/workspace-launcher.html`：打开文件夹/文件、最近工作区、过滤、过滤空态、失效记录、重新授权、会话恢复和加载状态。
- P3 布局事实源是 `prototypes/theme-preset-studio.html`；第一阶段不开发、不生成替代页面，也不预建无消费的主题组件。
- 第一阶段只消费 `DESIGN.md` alpha 语义 token，不在页面复制私有 hex、间距、圆角或状态色；状态不能只靠颜色表达。正式运行时 token 文件建立后，代码与自动化测试成为数值事实源，并同步设计文档。
- P1/P2 至少覆盖 `ready/loading/empty/error`，文件场景按需覆盖 `readonly/dirty/saving/conflict/missing/permission-denied`；阶段 1 不存在的编辑状态必须隐藏而非伪造。状态优先级为：安全阻塞状态（权限撤销/根失效）高于操作失败，高于 loading/processing，高于 empty/ready。
- 关键窗口验证遵循 `DESIGN.md`：完整桌面、约 `1050px`、约 `820px`，P2 另测约 `760px` 单列临界宽度；标题栏和主内容不得形成额外整页滚动。
- 对话框使用具体动作动词，支持 Esc、焦点圈定和关闭后焦点恢复；Toast 只用于已完成的轻量结果，不承载选择、失败恢复或长期状态。

### 2.3 推荐技术基线确认结果

- 桌面壳：Tauri 2。
- 前端：React + TypeScript + Vite 8，使用 pnpm，精确依赖由 `pnpm-lock.yaml` 锁定。
- 后端：Rust stable；工作区读写通过 Rust 命令层集中实现，前端不持有 `$HOME/**/*` 等宽泛文件系统权限。
- 系统桥接：按任务实际需要评估 Tauri dialog、single-instance、window-state、opener 等官方插件；删除到回收站使用通过许可证审查的跨平台 Rust 实现。T4 因原子替换要求采用 Rust 私有 JSON 仓储，没有注册 Tauri Store 插件或前端 Store 权限。
- 第一阶段辅助状态：版本化 Rust JSON 状态仓储；全文搜索阶段再评估 SQLite/FTS5，不在阶段 1 引入数据库。
- Markdown 内容事实源：用户工作区中的真实 `.md` 文件。

## 3. 实施范围

### 3.1 必须实现与本次纳入优化

需求第 10 章中标记“本次纳入”的 R8、R9、R13、R14、R15、R30、R31、R32 本身均属于必须实现项，沿用同一 R 编号，不另建增强编号；阶段外能力只登记义务，不在本计划预分配任务号。

| 需求编号 | 需求内容 | 当前阶段覆盖 | 开发状态 | 第一阶段任务编号 | 当前阶段验证方式 |
| --- | --- | --- | --- | --- | --- |
| R1 | Windows + macOS 桌面应用与本地优先工作区 | 桌面壳、真实选择器、授权根、离线启动、原生窗口和菜单；Windows 原生 UI 人工项不在本阶段伪称通过 | 进行中 | T1、T3、T5、T6、T11～T17 | T1 已验证工具链；T3 已验证 macOS 原生窗口、菜单与多窗口壳；T5 已验证选择/授权边界；T6 已验证授权工作区的后台只读扫描与读取命令核心，真实选择器 UI、工作区页面与窗口落地仍按 T11～T17 验证 |
| R2 | Markdown 文件与文件夹目录管理 | 扫描、读取、文件树、CRUD、回收站、系统定位和外部变化；不含编辑会话联动 | 进行中 | T2、T5～T10、T12～T17 | T2 已验证授权根、相对路径和错误契约；T6 已验证渐进扫描/读取；T7 已验证新建、重命名、移动、名称矩阵、无覆盖和失败保持；删除/回收站、监听及页面 E2E 仍按 T8～T10、T12～T17 验证 |
| R3 | Markdown 所见即所得查看与编辑 | 阶段外，需另立阶段计划 | 跳过 | - | 后续计划必须逐项承接需求 §11 的 R3 验收，不得复用第一阶段任务号 |
| R4 | 当前文档标题大纲 | 阶段外，需另立阶段计划 | 跳过 | - | 后续计划必须覆盖 H1～H6、空态、联动和异常验收 |
| R5 | 自动保存、恢复与外部修改冲突处理 | 仅安全写入与修订原语；不含自动保存、恢复快照和冲突 UI | 待开始 | T10、T15、T17 | 原子写故障注入、修订准确性、失败保持原文件；完整 R5 后续另验 |
| R6 | 图片粘贴、拖放与相对路径资源管理 | 阶段外，需另立阶段计划 | 跳过 | - | 后续计划必须覆盖资源目录、相对链接、失败回滚和跨平台验收 |
| R7 | 克制中性主题与颜色语义 | 只消费现有 `DESIGN.md` alpha token，不实现主题能力 | 跳过 | - | T12～T15 的设计合规检查不计为 R7 产品验收；完整 R7 后续按需求 §11 验收 |
| R8 | 自适应与区域拖动调宽；含按工作区记忆优化 | 仅保证 P1/P2 阶段壳在关键宽度不溢出，并实现 P1 窄窗文件树抽屉与 P2 约 760px 单列退化；拖动和偏好阶段外 | 待开始 | T12～T15、T17 | 完整桌面/1050px/820px/P2 760px 的主区、滚动、抽屉与焦点；完整 R8 后续另立计划 |
| R9 | 编辑、专注和分页阅读；含锚点恢复优化 | 阶段外，需另立阶段计划 | 跳过 | - | 后续计划必须覆盖模式、分页、不可裁切和语义锚点验收 |
| R10 | Markdown 源码模式 | 阶段外，需另立阶段计划 | 跳过 | - | 后续计划必须覆盖统一内容源和异常语法无损往返 |
| R11 | 操作与文件状态反馈 | 承接授权、扫描、文件操作、窗口创建和 P1/P2 页面真实状态子集 | 待开始 | T12～T15、T17 | 状态优先级、故障注入、恢复入口、非 toast 错误承接与文案断言 |
| R12 | 工作区全文搜索 | 阶段外，需另立阶段计划 | 跳过 | - | 后续计划必须覆盖中英文语料、定位、降级和重建验收 |
| R13 | 多文档页签与最近关闭；含独立会话优化 | 阶段外，第一阶段不得用单文档切换模拟 | 跳过 | - | 后续计划必须覆盖独立内容/模式/锚点、关闭保护和恢复验收 |
| R14 | 一目录一窗口与多窗口生命周期；含同目录聚焦优化 | 窗口注册、三选项目录决策、同目录聚焦和根工作区会话；不含页签保存检查和完整恢复 | 进行中 | T4、T5、T11～T17 | T4 已验证版本化根会话仓储；T5 已验证稳定工作区 ID、同根识别、最近记录复检和打开失败释放；窗口创建/聚焦、三选决策与部分恢复仍按 T11～T17 验证 |
| R15 | 可编辑颜色主题预设与实时预览；含内置只读优化 | 阶段外，P3 不开发 | 跳过 | - | 后续计划必须覆盖草稿隔离、预设 CRUD、校验、预览、回退和跨窗口同步 |
| R30 | 核心命令键盘操作；含平台快捷键与焦点恢复优化 | 只承接阶段 1 真实对话框、菜单和 P1/P2 可用操作的键盘链路，不承接编辑/页签/搜索/阅读快捷键 | 进行中 | T3、T12～T15、T17 | T3 已验证原生菜单快捷键登记与未实现命令禁用；Tab、Esc、对话框焦点恢复及页面可见焦点仍待后续任务 |
| R31 | 无障碍基础；含非颜色状态表达优化 | P1/P2 壳和公共组件子集 | 待开始 | T12～T15、T17 | 可访问树、键盘、焦点、aria-live、reduced-motion 和非颜色表达 |
| R32 | 长文阅读排版；含多 DPI 校准优化 | 阶段外；阶段 1 不建立正文编辑/阅读画布 | 跳过 | - | 后续计划必须覆盖中英文长文、DPI、字体回退和视觉验收 |

阶段外必须实现项与“本次纳入”优化仍保留产品义务，但本计划不为其创建空占位任务。进入相应阶段前，必须新建该阶段子目录与独立 `plan.md`，重新读取届时的代码事实，为相关 R 项分配该阶段真实任务编号、依赖、状态和验证方式。

### 3.2 可选增强

| 需求编号 | 增强内容 | 需求阶段处理状态 | 开发状态 | 对应任务 | 说明 |
| --- | --- | --- | --- | --- | --- |
| R16 | 导出 HTML/PDF | 后续建议 | 跳过 | - | 等核心编辑与分页渲染稳定后另立计划 |
| R17 | 本地历史版本与 Git 集成 | 后续建议 | 跳过 | - | 等自动保存和冲突模型稳定后另立计划 |
| R24 | 固定页签与预览页签组合追溯项 | 后续建议 | 跳过 | - | 仅保留追溯，子项由 R33、R34 承接，不直接开发 |
| R25 | 跨窗口拖拽页签 | 后续建议 | 跳过 | - | 文件归属和跨工作区语义未进入首版 |
| R26 | Plainroot 预设本地导入导出 | 后续建议 | 跳过 | - | 等 R15 预设格式、版本和冲突规则稳定 |
| R33 | 固定页签 | 后续建议 | 跳过 | - | 等 R13 基础页签状态机稳定 |
| R34 | 预览页签 | 后续建议 | 跳过 | - | 等 R13 基础页签状态机稳定 |

### 3.3 暂不实现

| 需求编号 | 内容 | 开发状态 | 跳过原因 |
| --- | --- | --- | --- |
| R18 | 双向链接知识图谱 | 跳过 | 首版不扩展为知识管理平台 |
| R19 | 云同步 | 跳过 | 不建立账号、服务端、加密和在线冲突闭环 |
| R20 | 多人实时协作 | 跳过 | 不建立实时协议和团队权限体系 |
| R21 | 插件市场 | 跳过 | 不建立扩展分发、安全和兼容生态 |
| R22 | 手机端 | 跳过 | 当前目标平台仅 Windows 与 macOS |
| R23 | 任意文字任意颜色 | 跳过 | 不引入非标准 Markdown 颜色语法 |
| R27 | 同一窗口附加多个根目录 | 跳过 | 保持一目录一窗口的权限、索引和会话边界 |
| R28 | 同一目录强制打开多个可写窗口 | 跳过 | 避免重复监听和并发覆盖 |
| R29 | Obsidian 主题源码兼容层 | 跳过 | 与 Plainroot 自有预设格式严格分离 |

## 4. 技术方案

### 4.1 工程结构与模块边界

计划采用以下目标结构；实际脚手架生成后如工具默认目录略有差异，只允许调整路径，不改变职责边界：

```text
.
├── .github/workflows/ci.yml
├── .nvmrc
├── package.json
├── pnpm-lock.yaml
├── rust-toolchain.toml
├── src/
│   ├── app/
│   ├── components/
│   ├── features/launcher/
│   ├── features/workbench/
│   ├── services/desktop/
│   └── test/
├── src-tauri/
│   ├── capabilities/
│   ├── src/
│   │   ├── commands/
│   │   ├── fs/
│   │   ├── menu.rs
│   │   ├── state.rs
│   │   ├── window.rs
│   │   └── lib.rs
│   ├── Cargo.toml
│   ├── Cargo.lock
│   └── tauri.conf.json
└── tests/fixtures/workspaces/
```

- React 只负责视图、用户意图和可观察状态，不直接拼接任意绝对路径执行写操作。
- Rust 命令层负责根目录授权登记、规范化路径、路径越界检查、文件扫描、CRUD、安全写入、回收站和窗口协调。
- `WorkspaceRegistry` 保存当前进程内 `canonicalRoot → windowLabel` 映射；Windows 路径进行大小写和前缀规范化，避免同一目录因文本差异被重复打开。
- 用户已确认选择单个 `.md` 文件时以其父目录作为工作区根目录，并把该文件作为初始活动目标；打开前必须显示将被授权和扫描的父目录路径，允许取消。若父目录已打开则只聚焦现有窗口。
- 同时保留用户选择的原始路径与解析后的 `canonicalRoot`。所选根本身是符号链接时，必须在授权提示中展示解析后的目标并经用户继续确认，去重与根内判定均使用该目标；根内符号链接节点默认不跟随，防止越过授权根或形成循环。
- 隐藏文件默认不展示；`showHidden=false` 是第一阶段命令默认值而非可持久化用户配置。设置入口和配置读写由后续独立阶段闭环实现。

### 4.2 第一阶段核心数据契约

| 模型 | 关键字段 | 生产者 | 消费者 | 校验与失败 |
| --- | --- | --- | --- | --- |
| `WorkspaceDescriptor` | `id`、`selectedPath`、`canonicalRoot`、`displayName`、`writable`、`initialFile` | Rust 授权服务 | P1、窗口协调器、状态仓储 | 根路径存在、可读且规范化；根为链接时展示解析目标并确认；失败返回稳定错误码 |
| `FsEntry` | `relativePath`、`name`、`kind`、`writable`、`symlink`、`childrenState` | 扫描服务 | 文件树 | 禁止返回根外绝对路径；不可读项带错误状态 |
| `FileRevision` | `modifiedAt`、`size`、`contentHash`、`encoding`、`lineEnding` | 读取/安全写服务 | 后续 R5 状态机 | 非 UTF-8 不覆盖；混合换行显式标记 |
| `RecentWorkspace` | `workspaceId`、`canonicalRoot`、`displayName`、`lastOpenedAt`、`availability` | 工作区服务 | P2 | 打开前重新检查，不信任历史可用状态 |
| `WorkspaceSessionRoot` | `workspaceId`、`windowLabel`、`windowStateRef`、`lastActiveAt` | 窗口协调器 | P2 根会话恢复 | 第一阶段不含 R13 页签集合；字段预留版本升级 |
| `DesktopError` | `code`、`messageKey`、`pathHint`、`contentSafe`、`retryable` | Rust/桥接层 | P1/P2 状态组件 | 不把原始系统堆栈直接展示给用户 |

### 4.3 安全写入与文件一致性

- 同目录创建临时文件，写入后 `flush`/必要时 `sync_all`，再执行平台适配的安全替换；新文件与覆盖文件分别处理。
- POSIX 使用同文件系统原子重命名；Windows 使用经过 PoC 验证的替换 API，目标被占用或替换失败时保留原文件并返回错误。
- 写入前后返回 `FileRevision`，供后续编辑冲突与全文搜索阶段通过稳定契约复用。
- 第一阶段只暴露“显式测试写入/文件操作”所需原语，不实现防抖自动保存，不把底层写入成功冒充 R5 整体验收通过。
- 新建、重命名、移动和删除均以磁盘成功为提交点；前端先显示进行中，成功后更新树，失败恢复旧状态。
- 删除优先进入系统废纸篓/回收站；能力不可用时必须由自定义对话框明确展示永久删除后果，用户二次确认后才调用永久删除。
- 文件监听事件合并并携带来源提示；应用自身操作通过操作令牌/短期事件登记去重，避免重复刷新和后续保存循环。
- 临时文件使用可识别且不与用户文件冲突的同目录命名；写入或替换失败先尝试删除。清理也失败时只在安全诊断日志记录脱敏引用，并在下一次应用启动执行有界重试；不得向 Toast 暴露绝对路径，也不得无限累积残件。

### 4.4 窗口、菜单与状态存储

- 启用 single-instance，确保一个进程协调多个工作区窗口；第二次启动或系统打开请求转交现有进程处理。
- 窗口标签使用稳定工作区 ID，不把完整路径直接作为标签；窗口标题遵循“工作区 — 当前文件 — Plainroot”，第一阶段没有活动文档时显示“工作区 — Plainroot”。
- 原生菜单实现“文件、编辑、显示、窗口、帮助”顶层结构：阶段 1 启用打开文件夹、打开 Markdown 文件、新建窗口、关闭窗口、在文件管理器显示等已实现命令；后续命令保持禁用并具有明确状态，不绑定空操作。
- 最近工作区和根窗口会话由 Rust 仓储保存到 `appDataDir()/plainroot-state-v1.json`；数据包含 `schemaVersion: 1`，损坏或超过 8 MiB 的文件先备份为 `.corrupt-{timestamp}` 再回退默认，未知版本原样保留且禁止覆盖。
- 第一阶段每次打开其他目录都显示“当前窗口 / 新窗口 / 取消”三选项，不提供“记住选择”，也不持久化打开方式。后续只有在真实设置页可读取、修改并恢复为“每次询问”时，才能把 R14 的默认偏好与设置回退作为同一任务实现。
- 默认初始化数据：最近工作区空数组、根窗口会话空数组；初始化发生在应用首次启动或状态损坏回退时。
- 第一阶段不创建 SQLite、SQL、字典、远端权限、账号或服务端初始化数据。

### 4.5 页面 P1：Markdown 编辑工作台技术实现

- 页面定位：工作区窗口内的文件管理壳；入口来自 P2、系统打开请求或新窗口。第一阶段覆盖 7.1.1、7.1.3 的最小壳、7.1.7 及 7.1.2 的安全写底座，不实现编辑器、页签、大纲、搜索、阅读、布局偏好和侧栏拖动。
- 原型及保留区域：读取 `prototypes/markdown-workbench.html`，保留原生窗口层、工作区标题、菜单、左文件树、中部空/读/错主区、状态栏和目录决策对话框；页签层、右大纲及未实现工具按钮不渲染。
- 数据与安全边界：Markdown/目录事实源来自 Rust command；前端只提交 `workspaceId + relativePath`。磁盘成功后才更新文件树；失败保留旧路径、旧选择和 `contentSafe` 状态。P1 与其他窗口只通过窗口注册表和版本化状态仓储共享根会话，不共享未实现的编辑状态。
- 布局与交互：宽窗口为左文件树 + 中央主区；约 1050px 仍保证中央主区且不出现整页滚动；约 820px 及以下将文件树变为可恢复抽屉，页签/右大纲因阶段外不渲染。文件树和中央区各自管理滚动。对话框使用“在当前窗口打开 / 在新窗口打开 / 取消”，支持 Esc、焦点圈定和焦点恢复。
- 复用与维护：消费 `AppDialog`、`AsyncStatePanel`、`PathStatus`、`DesktopWindowStatus`、desktop service 和 Rust command；不预建页签、编辑器、大纲或主题抽象。只消费 `DESIGN.md` 语义 token。

| 状态 | 触发条件 | 页面反馈 | 可执行操作 | 恢复/退出条件 |
| --- | --- | --- | --- | --- |
| ready | 根有效且扫描完成 | 文件树、工作区标题、稳定状态栏 | 导航、真实文件操作、打开其他目录 | 发起操作或根状态变化 |
| loading | 授权、扫描或窗口切换中 | 对象明确的进度，阻止重复提交 | 允许安全取消的流程显示取消 | 命令成功或失败 |
| empty | 根有效但没有受支持 Markdown | 明确空态和新建 Markdown 入口 | 新建文件、刷新、打开其他目录 | 出现有效条目 |
| error | 局部命令失败且根仍安全 | 就地错误、`contentSafe` 和重试入口 | 重试、取消、查看安全范围 | 重试成功或用户取消 |
| readonly | 根或节点不可写 | 文字+图标标记，只保留安全读取 | 重新授权、打开其他目录 | 权限恢复或离开工作区 |
| missing / permission-denied | 根失效或授权撤销 | 高于普通 error 的阻塞面板 | 重新授权、从最近记录移除、关闭窗口 | 授权成功或退出 |

| 页面功能点 | 对应需求编号 | 需求交互要点 | 技术实现 | 涉及组件/模块 | 数据/API | 状态与异常处理 | 对应任务 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 7.1.1 打开工作区与文件导航 | R1、R2、R11、R31 | 标题层、左文件树、新建入口和空态：打开、渐进树、CRUD 与失败恢复 | 系统选择器→Rust 授权→扫描/变更命令；磁盘成功后提交 UI | `WorkspaceTree`、`AsyncStatePanel`、launcher/workbench service | `open_folder`、`scan_workspace`、`read_file`、create/rename/move/trash/reveal、watch stream | loading/empty/readonly/missing/permission-denied；局部错误不吞掉有效节点 | T3、T5～T9、T13～T15 |
| 7.1.2 编辑、源码与保存 | R3、R5、R6、R10、R11、R30、R31 | 中央画布、模式工具和保存状态：第一阶段不显示未实现模式，只建立安全写原语 | 同目录临时写、平台安全替换和 `FileRevision`；无编辑 UI | Rust `safe_write`、`DesktopError` | `safe_write`（仅供集成测试和后续真实编辑消费） | 失败保持原文件；临时残件有界清理；不提示“已保存” | T10、T15（仅 R5 底座） |
| 7.1.3 自适应布局和调宽 | R8、R11、R30、R31 | 主工作区、左文件树和分隔区域：主区不溢出，窄窗文件树可恢复；P2 同步建立约 760px 响应式基线 | T12 完成 P2 单列退化；T13～T15 完成 P1 token 布局、≤820px 文件树抽屉和验证；拖动和偏好不实现 | P1/P2 shell、`WorkspaceTree`、drawer trigger | 窗口尺寸；无持久化偏好 API | 抽屉隐藏时有可发现恢复入口；焦点关闭后回触发点；T17 只基于真实证据验收 | T12～T15、T17（仅最小响应式壳） |
| 7.1.4 大纲、专注与分页阅读 | R4、R7、R9、R30～R32 | 右大纲、模式开关和分页状态：阶段外，不渲染入口或占位反馈 | 无 | 无 | 无 | 不创建假大纲、假分页或模式 Toast | -（后续另立计划） |
| 7.1.5 全文搜索 | R12、R30 | 搜索入口和结果弹层：阶段外，不渲染 | 无 | 无 | 无 | 不创建固定搜索结果或伪索引 | -（后续另立计划） |
| 7.1.6 多文档页签 | R5、R13、R30 | 页签条、溢出菜单和关闭保护：阶段外，不用内容切换模拟 | 无 | 无 | 无 | 不渲染假页签、溢出菜单或关闭成功 | -（后续另立计划） |
| 7.1.7 打开其他目录与多窗口 | R1、R5、R14、R30、R31 | 文件菜单、窗口层和目录决策对话框：每次三选、同目录聚焦、失败不破坏原窗口 | canonical registry、窗口命令、三选对话框；不持久化打开偏好 | `AppDialog`、WorkspaceRegistry、window service | `open_workspace`、`focus_workspace_window`、`create_workspace_window` | 取消无副作用；创建/授权失败保留原窗口；阶段 1 无编辑保存检查 | T5、T11、T13～T15 |

- 功能点验证：7.1.1 以临时目录和磁盘/UI 断言验证；7.1.2 只验证安全写故障原语；7.1.3 在完整桌面、1050px、820px 与 P2 约 760px 验证主区、滚动、键盘、焦点、抽屉恢复和单列退化，并由 T17 汇总验收；7.1.7 以多窗口 WDIO、canonical root 去重、取消/失败保留原窗口验证。7.1.4～7.1.6 本阶段验证入口不存在，后续独立计划必须重新给出正向验收。

### 4.6 页面 P2：工作区启动与最近目录技术实现

- 页面定位：无活动工作区时的启动页，主任务只有打开本地文件夹或 Markdown 文件；最近工作区与根窗口恢复是辅助入口。
- 原型及保留区域：读取 `prototypes/workspace-launcher.html`，保留打开入口、最近列表、过滤、空态、失效/重新授权和恢复状态；删除固定路径、计时器与演示按钮。原型工具栏“设置”按钮在第一阶段不渲染，因为没有可消费的设置页或回退闭环。
- 数据与安全边界：选择器结果交 Rust 校验；单文件打开先显示父目录授权范围；状态仓储只保存最近记录与根窗口会话。移除记录绝不删除本地文件，重新授权成功后继续原打开/恢复流程。
- 布局与交互：约 760px 以上双栏，以下单栏且打开入口排在最近列表之前；页面主区单一滚动，标题栏稳定。失败使用持久错误面板或对话框，Toast 只报告已完成的轻量移除/打开结果。
- 复用与维护：消费 P1 同一 `AppDialog`、`AsyncStatePanel`、`PathStatus`、desktop service 和状态 repository；不创建 P2 私有对话框或私有颜色体系。

| 状态 | 触发条件 | 页面反馈 | 可执行操作 | 恢复/退出条件 |
| --- | --- | --- | --- | --- |
| ready | 状态仓储可读且无进行中任务 | 打开入口与最近列表 | 打开、过滤、选择最近项 | 发起操作 |
| loading | 校验、打开或恢复中 | 显示具体目录/文件和进度 | 可安全取消时允许取消 | 成功进入 P1 或失败 |
| empty | 无最近记录 | 简洁空态，主打开入口仍可用 | 打开文件夹/文件 | 产生最近记录 |
| filtered-empty | 过滤无匹配 | 文案 + 可点击“清除过滤” | 清除过滤、打开新内容 | 清除或修改查询 |
| error | 状态仓储/打开/恢复局部失败 | 错误对象、`contentSafe`、重试/跳过 | 重试、重新授权、跳过 | 成功或用户退出 |
| missing / permission-denied | 最近路径失效或权限撤销 | 失效标记与明确后果对话框 | 重新授权、仅移除记录 | 校验成功或记录移除 |

| 页面功能点 | 对应需求编号 | 需求交互要点 | 技术实现 | 涉及组件/模块 | 数据/API | 状态与异常处理 | 对应任务 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 7.2.1 打开文件夹或 Markdown 文件 | R1、R14、R30、R31 | 两个打开主入口和加载对话框：真实选择、授权范围、取消、同目录聚焦 | Tauri dialog→Rust 校验；单文件展示父目录后授权；canonical root 去重 | launcher、`AppDialog`、desktop service | `open_folder`、`open_markdown_file`、`validate_workspace`、window commands | 取消不写状态仓储；链接根二次确认；失败保留启动页 | T5、T11、T12、T14、T15 |
| 7.2.2 最近工作区与失效记录 | R1、R14、R30、R31 | 最近列表、过滤/清除、过滤空态和失效对话框：仅按名称/路径过滤并安全移除 | 读取版本化状态仓储；成功打开后按最后打开时间排序并修剪至 100 条；移除只删记录；重新授权后续开流程 | `RecentWorkspaceList`、`PathStatus`、`AppDialog` | recent repository、`validate_workspace`、`remove_recent` | empty/filtered-empty/missing/permission-denied；修剪/移除均不删除本地文件 | T4、T5、T12、T14、T15 |
| 7.2.3 窗口会话恢复 | R14、R31 | 恢复进度和失败项：逐根恢复，单项失败不阻塞其他项 | 只恢复根工作区窗口并逐项校验；不恢复不存在的页签/锚点 | restore coordinator、`AsyncStatePanel` | session-root repository、window commands | partial-success、重新授权后继续、跳过、重试 | T4、T11、T12、T14、T15 |

- 功能点验证：7.2.1 覆盖选择/取消、父目录授权、链接根确认、同目录聚焦和失败保留；7.2.2 覆盖名称/路径过滤、可点击清除、100 条上限修剪、失效、重新授权后继续、移除前文案及磁盘不变；7.2.3 覆盖单项失败隔离、跳过、重试和 partial-success。另在完整桌面与约 760px 检查主任务顺序、滚动、焦点和文本溢出。

### 4.7 页面 P3：颜色主题预设工作室技术实现

- 原型文件：`prototypes/theme-preset-studio.html`。
- 第一阶段状态：跳过。P3 不注册路由、菜单、状态 schema、主题 command 或无消费组件；进入主题阶段时必须在独立计划中重新读取原型、`DESIGN.md` 和届时代码事实。

| 页面功能点 | 对应需求编号 | 需求交互要点 | 技术实现 | 涉及组件/模块 | 数据/API | 状态与异常处理 | 对应任务 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 7.3.1 选择、新建与复制预设 | R15 | 预设库和新增/复制入口：隔离草稿、内置只读、保存后入库 | 本阶段无 | 本阶段无 | 本阶段无 | 不渲染入口，不预注册空能力 | -（后续另立计划） |
| 7.3.2 编辑令牌与实时预览 | R7、R15、R31 | token 编辑区和实时预览区：明暗令牌、窗口内预览、校验 | 本阶段无 | 本阶段无 | 本阶段无 | `DESIGN.md` 的联合校验仍是待确认，不提前写成事实 | -（后续另立计划） |
| 7.3.3 保存、删除与回退 | R15 | 固定提交区和删除/回退对话框：原子保存、广播、删除前选择回退 | 本阶段无 | 本阶段无 | 本阶段无 | 不创建只有保存没有消费/回退的配置 | -（后续另立计划） |

- 第一阶段验证：断言没有 P3 路由、菜单、状态字段、command 和可点击入口。后续主题阶段必须为 7.3.1～7.3.3 分别补齐真实任务号、正向交互、失败回滚和验收证据。

### 4.8 组件资产闭环

| 页面/区域 | 运行时复用组件 | 布局或配置组件种类 | 配置/页面注册 | 设计规范或清单 | 真实消费与验证 | 对应任务 |
| --- | --- | --- | --- | --- | --- | --- |
| P1/P2 原生窗口状态 | 新增 `DesktopWindowStatus`、`AsyncStatePanel` | 不涉及配置化布局 | 窗口标签和标题由 desktop service 注入 | 在 `src/components/README.md` 登记职责 | P1、P2 同时消费；组件测试与 WDIO | T3、T14、T15 |
| P1/P2 对话框 | 新增可访问 `AppDialog` | 不涉及 | 焦点圈定、Esc、返回触发点 | 登记交互契约 | 目录决策、失效记录、永久删除真实消费 | T8、T11、T12、T14 |
| P1 文件树 | 新增 `WorkspaceTree` | 不涉及 | 消费 `FsEntry`，不自行执行磁盘乐观提交 | 登记数据/事件契约 | P1 真实目录消费；集成与 E2E | T6、T7、T9、T13 |
| P2 最近工作区 | 新增 `RecentWorkspaceList` | 不涉及 | 消费状态仓储 view model | 登记空/错/加载状态 | P2 真实状态仓储消费 | T4、T12、T15 |
| 阶段外三栏拖动、页签、编辑器、主题 | 本阶段不创建 | 本阶段不创建 | 不预注册无消费资产 | 否，避免空抽象 | 后续独立计划基于届时代码重新做复用检查 | - |

## 5. 需求与开发内容映射和开发状态

本表中的“状态”仅表示第一阶段，不把产品级必须实现项标成永久取消；“阶段外”统一为跳过，且不得在本计划伪造未来任务编号。

| 范围类别 | 需求编号 | 需求内容/来源 | 第一阶段开发内容 | 第一阶段任务 | 状态 | 偏差判断 | 验证与后续约束 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 必须实现 | R1 | 双平台桌面应用与授权工作区 | 桌面壳、授权、窗口、菜单、P1/P2 与双平台 CI | T1、T3、T5、T6、T11～T17 | 进行中 | 部分覆盖 | T1/T3/T5/T6 已完成工具链、macOS 原生壳、授权选择和只读工作区扫描/读取核心；其余验证见 §8，Windows 原生 UI 未取得证据前不标完整完成 |
| 必须实现 | R2 | 文件与文件夹目录管理 | 文件层扫描、读取、CRUD、删除、定位与监听 | T2、T5～T10、T12～T17 | 进行中 | 部分覆盖 | T2/T5/T6/T7 已完成路径契约、授权、扫描/读取及新建/重命名/移动底座；删除、监听与 P1 真实页面仍待 T8～T10、T12～T17，编辑会话耦合留后续独立计划 |
| 必须实现 | R3 | 所见即所得编辑 | 阶段外 | - | 跳过 | 未覆盖 | 后续独立计划按需求 §11 验收 |
| 必须实现 | R4 | 当前文档大纲 | 阶段外 | - | 跳过 | 未覆盖 | 后续独立计划按需求 §11 验收 |
| 必须实现 | R5 | 自动保存、恢复与冲突 | 仅安全写入与修订底座 | T10、T15、T17 | 待开始 | 部分覆盖 | 故障注入验证不破坏原文件；完整状态机后续另立计划 |
| 必须实现 | R6 | 图片和资源 | 阶段外 | - | 跳过 | 未覆盖 | 后续独立计划按需求 §11 验收 |
| 必须实现 | R7 | 主题颜色语义 | 只消费 `DESIGN.md` token，不开发主题能力 | - | 跳过 | 未覆盖 | T12～T15 只做设计合规检查；完整 R7 后续另立计划 |
| 必须实现/本次纳入 | R8 | 自适应、调宽和布局偏好 | P1/P2 最小响应式壳；不含拖动和偏好 | T12～T15、T17 | 待开始 | 部分覆盖 | 完整桌面/1050/820/P2 760px；完整 R8 后续另立计划 |
| 必须实现/本次纳入 | R9 | 专注、分页阅读和锚点 | 阶段外 | - | 跳过 | 未覆盖 | 后续独立计划按需求 §11 验收 |
| 必须实现 | R10 | Markdown 源码模式 | 阶段外 | - | 跳过 | 未覆盖 | 后续独立计划按需求 §11 验收 |
| 必须实现 | R11 | 真实状态反馈 | 授权、文件、窗口和 P1/P2 状态子集 | T12～T15、T17 | 待开始 | 部分覆盖 | 状态优先级、错误恢复、非 Toast 承接和故障注入 |
| 必须实现 | R12 | 全文搜索 | 阶段外 | - | 跳过 | 未覆盖 | 后续独立计划按需求 §11 验收 |
| 必须实现/本次纳入 | R13 | 多页签、最近关闭和独立会话 | 阶段外 | - | 跳过 | 未覆盖 | 后续独立计划分配真实任务号并按需求 §11 验收 |
| 必须实现/本次纳入 | R14 | 一目录一窗口、多窗口和打开偏好 | 三选目录决策、同目录聚焦、根窗口会话；不持久化打开偏好 | T4、T5、T11～T17 | 进行中 | 部分覆盖 | T4 已完成状态仓储，T5 已完成受控选择令牌、同根识别和最近记录复检；窗口协调、页面恢复与打开偏好仍按对应任务承接，偏好必须与真实设置重置入口同阶段实现 |
| 必须实现/本次纳入 | R15 | 可编辑主题预设 | 阶段外，P3 不注册 | - | 跳过 | 未覆盖 | 后续独立计划按 7.3.1～7.3.3 和需求 §11 验收 |
| 可选增强 | R16 | 导出 HTML/PDF | 不纳入 | - | 跳过 | 未覆盖 | 等编辑与分页渲染稳定后再评估 |
| 可选增强 | R17 | 历史版本与 Git | 不纳入 | - | 跳过 | 未覆盖 | 等保存和冲突模型稳定后再评估 |
| 暂不实现 | R18 | 双向链接知识图谱 | 不开发 | - | 跳过 | 未覆盖 | 需求 §4.3 排除：首版不扩展为知识管理平台 |
| 暂不实现 | R19 | 云同步 | 不开发 | - | 跳过 | 未覆盖 | 需求 §4.3 排除：不建立账号和在线冲突闭环 |
| 暂不实现 | R20 | 多人实时协作 | 不开发 | - | 跳过 | 未覆盖 | 需求 §4.3 排除：不建立实时协议和团队权限 |
| 暂不实现 | R21 | 插件市场 | 不开发 | - | 跳过 | 未覆盖 | 需求 §4.3 排除：不建立扩展分发生态 |
| 暂不实现 | R22 | 手机端 | 不开发 | - | 跳过 | 未覆盖 | 需求 §4.3 排除：目标平台仅 Windows/macOS |
| 暂不实现 | R23 | 任意文字任意颜色 | 不开发 | - | 跳过 | 未覆盖 | 需求 §4.3 排除：不引入私有 Markdown 颜色语法 |
| 可选增强 | R24 | 固定/预览页签组合追溯项 | 不直接开发 | - | 跳过 | 未覆盖 | 只做追溯，原子项为 R33/R34 |
| 可选增强 | R25 | 跨窗口拖拽页签 | 不纳入 | - | 跳过 | 未覆盖 | 等文件归属和跨工作区语义确认 |
| 可选增强 | R26 | 预设本地导入导出 | 不纳入 | - | 跳过 | 未覆盖 | 等 R15 格式与版本规则稳定 |
| 暂不实现 | R27 | 同窗附加多个根目录 | 不开发 | - | 跳过 | 未覆盖 | 需求 §4.3 排除：保持一目录一窗口边界 |
| 暂不实现 | R28 | 同目录多个可写窗口 | 不开发 | - | 跳过 | 未覆盖 | 需求 §4.3 排除：避免并发覆盖 |
| 暂不实现 | R29 | Obsidian 主题源码兼容层 | 不开发 | - | 跳过 | 未覆盖 | 需求 §4.3 排除：与自有预设格式分离 |
| 必须实现/本次纳入 | R30 | 核心命令键盘操作 | 阶段 1 真实菜单、对话框和 P1/P2 操作子集 | T3、T12～T15、T17 | 进行中 | 部分覆盖 | T3 已完成原生菜单启用态和新建/关闭窗口快捷键；Tab/Esc/对话框焦点恢复仍待后续任务，编辑/页签/搜索/阅读快捷键后续另验 |
| 必须实现/本次纳入 | R31 | 无障碍基础 | P1/P2 壳和公共组件子集 | T12～T15、T17 | 待开始 | 部分覆盖 | a11y tree、非颜色状态、aria-live、reduced-motion 和焦点 |
| 必须实现/本次纳入 | R32 | 长文阅读排版 | 阶段外 | - | 跳过 | 未覆盖 | 后续独立计划按需求 §11 与多 DPI 验收 |
| 可选增强 | R33 | 固定页签 | 不纳入 | - | 跳过 | 未覆盖 | 等 R13 基础页签状态机稳定 |
| 可选增强 | R34 | 预览页签 | 不纳入 | - | 跳过 | 未覆盖 | 等 R13 基础页签状态机稳定 |

## 6. 详细实施步骤与进度追踪

### 6.1 任务 T1：工具链、依赖 PoC 与 Tauri 工程骨架

- 状态：已完成。
- 依赖：无。
- 涉及文件/模块：`.gitignore`、`.claude/settings.json`、`.nvmrc`、`README.md`、`package.json`、`pnpm-lock.yaml`、`rust-toolchain.toml`、`index.html`、`src/`、`tsconfig*.json`、`vite.config.ts`、`scripts/check-licenses*.mjs`、`scripts/fixtures/`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/build.rs`、`src-tauri/src/`、`src-tauri/capabilities/`、`src-tauri/icons/`、`src-tauri/tauri.conf.json`。
- 目标：在本机现有 Node 24.11.1、pnpm 和经 PoC 确认的 Rust stable 版本上建立可重复构建的 Tauri 2 + React + TypeScript + Vite 8 工程。
- 操作：用 `.nvmrc` 和 CI 固定 Node 24.11.1，`engines.node` 声明兼容范围，`packageManager` 固定 pnpm 精确版本；安装 Rust stable、rustfmt、clippy，并在 PoC 通过后把 `rust-toolchain.toml` 固定到实际验证版本；脚手架初始化；验证 Tauri/Rust/React/Vite/plugin/候选回收站库许可证、包体和兼容性；提交锁文件。
- 产出：可运行桌面空壳、锁文件、依赖与许可证清单、PoC 结论。
- 影响范围：全项目开发与 CI 基线。
- 边界与异常：不安装 Milkdown、CodeMirror 或 SQLite；依赖 PoC 失败时回到候选技术确认，不静默替换框架。
- 验证方式：`nvm use`、`pnpm install --frozen-lockfile`、`pnpm build`、`pnpm test:licenses`、`pnpm licenses:check`、`cargo check --locked --manifest-path src-tauri/Cargo.toml`、`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`、`cargo clippy --locked --manifest-path src-tauri/Cargo.toml -- -D warnings`、`pnpm tauri build --no-bundle`。
- 完成标准：全新检出可按 README 命令构建；本地实际 Node 为 24.11.1，并提供供 T16 CI 消费的同一精确版本钉子；Rust/Cargo 和前端锁文件可复现。CI 实际版本只能在依赖 T1 的 T16 中验证，不反向阻塞 T1。
- 实际落地情况：已建立 Tauri 2.11.5 + React 19.2.7 + TypeScript 6.0.2 + Vite 8.1.4 空壳，锁定 Node 24.11.1、pnpm 11.5.1 与 Rust 1.97.1；提交双锁文件、最小 capability/CSP、README、许可证审计脚本及拒绝策略测试。macOS arm64 已通过 frozen-lockfile 安装、前端构建、许可证策略测试、Cargo check/fmt/clippy、完整依赖许可证扫描及 `tauri build --no-bundle`；后续插件与 `trash` 仅在临时 PoC 编译，不提前注册到正式应用。T1 的 800×600 仅为当时的无页面空壳占位，T3 已将阶段实现默认值调整为 1100×720、最小 720×520；`DESIGN.md` 的 1180/1050/820/760 仍是响应式验证断点，T12/T13 负责断点矩阵验收。详细证据见 `t1-toolchain-poc.md`。

### 6.2 任务 T2：领域契约、路径安全与错误模型

- 状态：已完成。
- 依赖：T1。
- 涉及文件/模块：`src-tauri/src/fs/`、`src-tauri/src/state.rs`、`src/services/desktop/`、共享 TypeScript 类型。
- 目标：建立 4.2 的模型和稳定错误契约。
- 操作：定义 Workspace/FsEntry/FileRevision/DesktopError；实现路径规范化、根内判定、Windows 大小写与前缀归一；分别保留所选根和 canonical root；明确“所选根本身为链接时展示解析目标并确认、根内链接默认不跟随”；生成前端类型或契约测试。
- 产出：Rust 领域模型、序列化契约、前端适配类型和错误码清单。
- 影响范围：所有文件、窗口和状态功能。
- 边界与异常：错误消息不泄露无关绝对路径；相对路径中 `..`、绝对路径注入和链接越界均拒绝；同一链接根和真实路径使用 canonical root 去重。
- 验证方式：Rust 参数化单测覆盖 macOS/Windows 路径样例、越界、大小写、根本身为链接、根内链接越界/循环和不可读路径。
- 完成标准：每个前端文件操作都只能基于已登记 workspaceId 和根内相对路径执行。
- 实际落地情况：已实现 Rust `WorkspaceDescriptor`、`FsEntry`、`FileRevision`、最近工作区/根会话模型、`DesktopError` 稳定错误码、校验式 `WorkspaceId`/相对路径反序列化、授权根解析与 canonical identity、Windows 前缀/分隔符/大小写身份归一和进程内 `WorkspaceRegistry`。根链接必须显式确认，根内链接默认拒绝，链接根与真实根按 canonical root 去重；Windows 对外路径去除扩展前缀，内部仍保留 canonical `PathBuf`。Rust 序列化字段与 TypeScript interface、Rust 错误码全集与前端常量均由自动契约测试防漂移。macOS arm64 的 17 个 Rust 单测及 TypeScript 构建已通过；真实 Windows 文件系统行为仍由 T16 验证。详细清单见 `t2-domain-contract.md`。

### 6.3 任务 T3：原生桌面壳、窗口标题和菜单

- 状态：已完成。
- 依赖：T1、T2。
- 涉及文件/模块：`src-tauri/src/lib.rs`、`src-tauri/src/menu.rs`、`src-tauri/src/window.rs`、`src-tauri/tauri.conf.json`、`src-tauri/capabilities/`。
- 目标：建立 R1 的原生窗口、系统控制、标题、顶层菜单和离线启动能力。
- 操作：配置启动窗口；实现 macOS/Windows 原生菜单惯例；只启用已落地菜单；接入新建/聚焦/关闭窗口事件；设置 CSP 和最小权限。
- 产出：P1/P2 可复用原生窗口壳与菜单命令总线。
- 影响范围：应用生命周期和全部页面入口。
- 边界与异常：不自绘替代系统控制；禁用菜单不得有空成功反馈；窗口创建失败返回真实错误。
- 验证方式：Rust 窗口协调单测、macOS 实机窗口冒烟、Windows CI Tauri E2E。
- 完成标准：原生窗口可创建、聚焦和关闭，标题与菜单状态真实。
- 实际落地情况：已建立独立 `window` 与 `menu` 服务；启动窗口和动态 launcher 窗口统一使用系统装饰、1100×720 默认尺寸、720×520 最小尺寸、居中与可缩放配置。窗口标题函数覆盖 `Plainroot`、`工作区 — Plainroot`、`工作区 — 当前文件 — Plainroot`；新窗口分配唯一 `launcher-N` 标签，可按标签聚焦/关闭，失败映射为四个稳定窗口错误码并由 Rust↔TypeScript parity 测试约束。macOS 提供应用/文件/编辑/显示/窗口/帮助原生菜单，非 macOS 保持文件/编辑/显示/窗口/帮助结构；仅新建窗口、关闭窗口及系统真实窗口操作启用，打开、编辑、搜索与帮助等未实现命令保持禁用且无事件处理。CSP 补充 `object-src`、`base-uri`、`frame-ancestors` 限制，capability 只覆盖 `main` 与 `launcher-*` 且仍仅授予 `core:default`。21 个 Rust 单测、clippy、前端构建和 macOS `.app` 构建通过；实机验证原生控件、菜单禁用态、新建第二窗口及关闭后保留原窗口。Windows 原生菜单与窗口行为仍由 T16 验证。详细证据见 `t3-native-window-shell.md`。

### 6.4 任务 T4：版本化本地状态和初始化数据

- 状态：已完成。
- 依赖：T1、T2。
- 涉及文件/模块：`src-tauri/src/state.rs`、`src-tauri/src/error.rs`、`src-tauri/src/lib.rs`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src/services/desktop/contracts.ts`、`plainroot-state-v1.json` schema/tests。
- 目标：保存最近工作区和根窗口会话，不引入数据库或没有 UI 回退入口的打开偏好。
- 操作：定义 `schemaVersion: 1`；初始化空状态；实现读取、原子更新、损坏备份、默认回退和未来迁移入口。
- 产出：进程内托管的 Rust 状态仓储和私有 JSON 状态文件。
- 影响范围：P2、R14 窗口协调。
- 边界与异常：状态写失败不影响 Markdown 文件；写盘成功前不提交内存状态；历史路径打开前重新校验；状态文件不写入工作区；未知版本和无法安全备份的状态不得被默认值覆盖。
- 验证方式：首次启动、读写、损坏 JSON、未知版本、不可写 appData 集成测试。
- 完成标准：损坏状态可恢复，最近记录和根窗口会话可重复读取，失败有明确状态；schema 中不存在打开方式偏好字段。
- 实际落地情况：已实现 `PlainrootStateV1`、`PersistentAppState` 与稳定状态错误码，并在 Tauri setup 时初始化/托管。首次启动写入空数组；同目录临时文件经 `sync_all` 后执行平台原子替换，Unix 新文件权限为 `0600`，Windows 适配使用 `MoveFileExW(REPLACE_EXISTING | WRITE_THROUGH)`。损坏、结构非法、重复 ID/窗口标签或超过 8 MiB 的文件先改名备份再初始化；未知 schema 版本保持原文件并阻止更新；写入失败保持磁盘和内存旧状态且清理临时文件。Tauri Store 插件当前保存路径不满足本任务原子替换要求，因此未注册插件、未扩大 capability，而是使用 Rust 私有仓储。30 个 Rust 单测、clippy、TypeScript 构建、许可证策略/全量扫描和 Tauri release 无 bundle 构建已通过；Windows 分支仅完成源码/API 核对，真实编译与文件系统行为留给 T16。详细证据见 `t4-versioned-local-state.md`。

### 6.5 任务 T5：系统选择器、工作区授权与打开管线

- 状态：已完成。
- 依赖：T2～T4。
- 涉及文件/模块：`src-tauri/src/commands/workspace.rs`、`src-tauri/src/commands/mod.rs`、`src-tauri/src/fs/model.rs`、`src-tauri/src/state.rs`、`src-tauri/src/error.rs`、`src-tauri/src/lib.rs`、`src/services/desktop/contracts.ts`、`src/services/desktop/workspace.ts`、Dialog plugin/锁文件。
- 目标：完成真实目录/文件选择、授权校验、最近记录更新和单文件父目录模型。
- 操作：调用系统选择器；取消不写状态；规范化与校验路径；单文件限定 `.md`，在进入扫描前显示父目录授权范围并允许取消；根为符号链接时展示解析目标并再次确认；同目录请求交给 T11；窗口成功后再由 Rust 内部提交最近记录。
- 产出：`select_workspace_folder`、`select_markdown_file`、`authorize_workspace_selection`、`cancel_workspace_selection`、`validate_recent_workspace` 命令及前端类型化调用封装。
- 影响范围：P1/P2 入口、R1/R14。
- 边界与异常：路径失效、权限撤销、不支持扩展名、根符号链接、链接越界和已打开目录均使用不同错误/决策状态；未经范围提示不得扫描单文件父目录。
- 验证方式：mock dialog、临时目录集成、macOS 原生选择器冒烟、Windows 命令层与 WDIO 测试。
- 完成标准：选择/取消/失败/聚焦/成功均与真实状态一致。
- 实际落地情况：已注册官方 `tauri-plugin-dialog` 2.7.1，但系统选择器只由 Rust command 发起，未给前端 Dialog/FS capability，也不接受前端绝对路径。`select_workspace_folder`、`select_markdown_file` 返回 `cancelled/already_open/ready/confirmation_required` 受控结果；单文件限定大小写不敏感的 `.md`，以父目录为根并始终要求范围确认，根为符号链接时展示 selected/canonical 路径并要求确认，文件链接直接拒绝。待确认提案使用操作系统随机的一次性令牌，10 分钟过期且最多 32 个；授权前重新校验路径与链接目标，换靶即失效。工作区 ID 使用 canonical identity 的版本化 SHA-256 摘要，同根返回现有 workspace；窗口打开失败可释放授权。POSIX 初始只读提示依据权限位，Windows 不把目录 readonly 属性误当 ACL，实际文件命令仍是写权限事实源。最近记录只由 Rust 内部 `record_workspace_opened` 在 T11 窗口成功后提交，现阶段菜单入口继续禁用，避免无页面确认或虚假完成。最近记录可按 workspace ID 重新校验，不接受前端历史路径。选择枚举和 tagged union 的状态/字段已加入 Rust↔TypeScript 自动防漂移。44 个 Rust 单测、clippy、前端构建、2 个许可证策略测试和 29/431/0 全量许可证扫描通过；测试线程、临时主循环及临时真实入口均未取得可复现的 macOS 原生面板回调，相关临时代码已移除，因此真实 macOS/Windows 选择器 UI 明确保留为 T12/T15/T16 未验证项。详细证据见 `t5-workspace-selection.md`。

### 6.6 任务 T6：目录扫描、文件读取和渐进文件树模型

- 状态：已完成。
- 依赖：T2、T5。
- 涉及文件/模块：`src-tauri/src/fs/scan.rs`、`src-tauri/src/fs/read.rs`、`src-tauri/src/commands/files.rs`、Rust command 注册与错误模型、`src/services/desktop/contracts.ts`、`src/services/desktop/files.ts`、前端 `WorkspaceTree` state。
- 目标：在不阻塞首屏的前提下展示工作区内文件夹和 Markdown 文件。
- 操作：后台分批扫描；默认隐藏隐藏项；只返回文件夹和受支持 Markdown；读取文件修订、UTF-8/换行信息；支持取消与手动刷新。
- 产出：扫描/读取命令、渐进树状态、测试夹具。
- 影响范围：R1、R2、P1.1。
- 边界与异常：数千文件显示进度；不可读节点局部失败；非 UTF-8 标记且禁止覆盖；链接不跟随。
- 验证方式：空目录、深目录、千文件、权限错误、CRLF/LF、非 UTF-8、链接循环测试。
- 完成标准：树与磁盘一致，扫描不阻塞窗口交互，错误不吞掉其他有效项。
- 实际落地情况：已新增后台 `WorkspaceScanService`，每个授权工作区/目录以独立 worker 分批扫描，前端通过 start/poll/cancel 命令消费；同目录重新开始会取消旧会话，活动扫描上限 16，通道有界，避免未轮询时无界积压。根和懒加载子目录只返回文件夹与大小写不敏感的 `.md`，默认排除隐藏项、非 Markdown 和不可表达路径；符号链接在临近 I/O 时再次检查、只报告局部问题且不跟随。前端 `WorkspaceTreeState` 以 scanId 防止过期批次回写，刷新时清理目标子树，保留进度、局部错误、完成/取消状态；复审后新增 4 个 Node 24 原生回归测试，覆盖过期批次、根/子树重扫和局部错误派生，不提前引入 T15 才需统一评估的测试框架。Markdown 读取仅接受已登记 workspaceId 与根内相对路径，在 blocking worker 中流式计算 SHA-256、UTF-8/BOM、LF/CRLF/CR/Mixed 和修改时间；64 MiB 是当前内联正文安全上限，超限返回 `too_large`，非 UTF-8 返回 `unsupported_encoding`，两者均不返回可编辑内容；就绪内容直接移动缓冲区构造 String，避免再复制至多 64 MiB。读取期间尺寸或修改时间变化返回可重试错误。`MarkdownReadStatus` 枚举值、对象字段和错误码均有 Rust↔TypeScript parity 测试；文件树与工作区 writable 提示复用同一跨平台规则，Windows 不把 readonly 属性误当 ACL。macOS arm64 已通过 55 个 Rust 测试、4 个前端状态测试、clippy 与 TypeScript 生产构建；Windows 隐藏属性/提示分支、真实 Tauri IPC、P1 文件树页面与真实大目录交互仍分别由 T16、T12/T15 验证。详细证据见 `t6-directory-scan-read.md`。

### 6.7 任务 T7：文件/文件夹新建、重命名和移动

- 状态：已完成。
- 依赖：T2、T6。
- 涉及文件/模块：`src-tauri/src/fs/mutate.rs`、`src-tauri/src/commands/files.rs`、Rust/TypeScript 契约与错误码、`src/services/desktop/files.ts`、前端 `WorkspaceTree` mutation state；P1 文件树控件和对话框由 T13 消费。
- 目标：实现 R2 的可验证磁盘操作闭环。
- 操作：校验空名、重名、平台保留名、非法字符、扩展名和权限；新文件默认 `.md`；磁盘成功后更新树；跨设备/越界移动明确失败。
- 产出：create/rename/move 命令、校验器、前端进行中/失败状态。
- 影响范围：工作区磁盘和文件树。
- 边界与异常：失败保持旧路径和选中状态；不覆盖目标；文件系统重命名不进入正文撤销栈。
- 验证方式：Rust 集成测试、并发重名、只读目录、路径越界、Windows 保留名矩阵、P1 E2E。
- 完成标准：磁盘提交点和 UI 状态一致，无虚假成功或覆盖。
- 实际落地情况：已新增串行 `WorkspaceMutationService` 与 create Markdown/create directory/rename/move 四个受控命令，前端只传 workspaceId、相对路径和名称。名称使用双平台安全子集：拒绝空白边界、控制符、路径分隔符、Windows 非法/保留名、超长项和当前不可见的点号隐藏项；Markdown 无扩展名补 `.md`，其他扩展名拒绝。创建使用 `create_new`，同步或元数据提交失败会清理未提交空文件；目录创建失败不产生前端成功。macOS 使用 `renamex_np(RENAME_EXCL)`，Windows 使用不带 replace 标志的 `MoveFileExW`，目标存在绝不覆盖；同一文件身份允许大小写改名，不同实体仍拒绝；跨设备、同位置和目录移入自身/后代使用独立稳定错误。所有操作在临近 I/O 时复核授权根、相对路径和符号链接，成功后返回真实 `FsEntry`；前端只有收到 Rust 成功结果且 mutationId 仍为当前操作时才新增或重映射树，失败只保留 processing/failed 状态，旧路径和子树不变，跨父目录移动会从旧父项移除并加入新父项。新增变更种类与错误码 parity。macOS 已通过 66 个 Rust 单测和 9 个前端树状态测试；真实 IPC/P1 控件与对话框仍由 T13/T15，Windows no-replace 分支由 T16 验证。详细证据见 `t7-workspace-mutations.md`。

### 6.8 任务 T8：回收站删除、永久删除回退与系统定位

- 状态：待开始。
- 依赖：T2、T7。
- 涉及文件/模块：Rust platform/trash adapter、opener bridge、可访问确认对话框。
- 目标：优先安全删除并提供“在文件管理器中显示”。
- 操作：调用平台回收站；失败时返回 `trash_unavailable`，由明确永久删除对话框二次确认；实现 reveal 命令。
- 产出：trash/permanent-delete/reveal adapter 和 P1 对话框。
- 影响范围：真实工作区文件。
- 边界与异常：永久删除按钮写明结果；取消不改变磁盘；删除最近记录与删除工作区文件严格分离。
- 验证方式：临时目录集成、macOS Finder/废纸篓人工冒烟、Windows CI adapter 测试；Windows 系统 UI 作为最终跨平台阶段的人工验收输入，在本阶段明确记为未验证。
- 完成标准：默认删除进入回收站；降级路径无隐式永久删除。
- 实际落地情况：待实施。

### 6.9 任务 T9：外部变化监听与文件树一致性

- 状态：待开始。
- 依赖：T6～T8。
- 涉及文件/模块：`src-tauri/src/fs/watch.rs`、前端 tree event reducer。
- 目标：增量反映外部新增、删除、移动和重命名。
- 操作：后台 watcher；事件合并；应用自身操作去重；尽量保持展开与选择；提供 watcher 失效后的重试/手动刷新。
- 产出：workspace event stream 和 reducer。
- 影响范围：R2 文件树、后续搜索和冲突状态机。
- 边界与异常：事件风暴防抖；根目录失效停止监听；当前编辑文档恢复属于后续独立阶段，本任务不预建竞争状态。
- 验证方式：外部进程变更夹具、批量事件、根目录删除、watcher 重启测试。
- 完成标准：外部变化最终与磁盘一致，自身操作不重复闪动。
- 实际落地情况：待实施。

### 6.10 任务 T10：安全写入和文件修订底座

- 状态：待开始。
- 依赖：T2、T6。
- 涉及文件/模块：`src-tauri/src/fs/safe_write.rs`、platform replace adapter、故障注入测试。
- 目标：为 R5 提供不会中途截断原文件的写入原语。
- 操作：同目录临时文件、flush/sync、平台安全替换、修订摘要、换行保留、非 UTF-8 阻断；设计可注入失败点。
- 产出：`safe_write` 接口和双平台测试。
- 影响范围：真实 Markdown 文件；后续编辑与搜索阶段通过稳定接口复用。
- 边界与异常：写入/替换/权限/占用失败保留原文件；临时文件先即时清理，清理失败只记录脱敏引用并在下次启动有界重试；不实现自动保存 UI。
- 验证方式：原文件前后哈希、断点故障、占用、只读、CRLF、目标不存在、临时文件清理成功/失败/启动重试和 Windows runner 测试。
- 完成标准：任一失败点不产生截断、虚假成功或无限残件，成功后修订信息准确。
- 实际落地情况：待实施。

### 6.11 任务 T11：工作区窗口协调与目录决策壳

- 状态：待开始。
- 依赖：T3～T5。
- 涉及文件/模块：`src-tauri/src/window.rs`、WorkspaceRegistry、P1 目录决策对话框。
- 目标：实现一目录一窗口、同目录聚焦及当前/新窗口/取消。
- 操作：注册 canonical root；每次处理当前窗口/新窗口/取消三选；把启动窗口 `main` 与临时 `launcher-N` 收敛为统一、可迁移的稳定窗口标签体系，并同步 capability 和根会话引用；根工作区会话持久化；第二实例转发打开请求；将 `window_not_found`、`window_create_failed`、`window_focus_failed`、`window_close_failed` 从菜单日志边界接入可被页面消费的窗口协调结果。本阶段不提供“记住选择”或打开偏好状态字段。
- 产出：窗口协调器和目录决策流程。
- 影响范围：R1、R14、多窗口生命周期。
- 边界与异常：阶段 1 没有编辑会话阻塞；完整编辑/页签阶段接入全部页签保存门禁后才能宣称当前窗口替换保护完成；标签迁移不得产生重复可写会话或失效 capability；创建失败保留原窗口并返回稳定错误，不得只停留在 stderr。
- 验证方式：多窗口 Rust/WDIO、启动窗口与动态窗口标签迁移、capability 覆盖、同目录不同路径表达、四类窗口错误、取消和状态仓储失败测试。
- 完成标准：同一 canonical root 不产生第二个可写窗口；原窗口在新窗口失败时保持不变；窗口协调调用方能够取得稳定错误对象。
- 实际落地情况：待实施。

### 6.12 任务 T12：P2 启动页与最近目录完整样板接入

- 状态：待开始。
- 依赖：T4、T5、T11。
- 涉及文件/模块：`src/features/launcher/`、路由/启动状态。
- 目标：把 P2 原型替换为真实应用数据和命令。
- 操作：先执行页面开发流程的定位、状态、提交边界和复用检查；实现打开入口、最近列表、名称/路径过滤、可点击清除、空态、加载、失效、重新授权后继续、安全移除和根窗口会话恢复；最近记录最多保留 100 条，每次成功打开后按 `lastOpenedAt` 降序修剪最旧项并以仓储测试锁定上限；消费 T11 的稳定窗口错误并以持久错误面板或对话框提供重试/取消，不用 toast 或 stderr 代替用户反馈；为首个真实场景建立对话框/异步状态候选组件；隐藏无真实设置页的原型设置按钮；按 `DESIGN.md` 消费 token 并实现约 760px 单列退化。
- 产出：可用启动页。
- 影响范围：首次启动、新窗口、无工作区和恢复失败流程。
- 边界与异常：移除前明确“不删除本地文件”；最近记录修剪只删除辅助记录、不删除目录或 Markdown；重新授权成功后继续原流程；根会话恢复不伪称恢复页签；设置按钮不绑定空行为；所有异步反馈基于真实命令结果。
- 验证方式：React 组件测试、Tauri WDIO、macOS 实机选择器；注入四类窗口错误并断言错误面板、重试/取消和焦点恢复；完整桌面和约 760px 检查主任务顺序、滚动、焦点、状态和文本溢出。
- 完成标准：7.2.1～7.2.3 第一阶段边界内状态均可观察、可取消、可重试；窗口动作失败不只写入开发日志。
- 实际落地情况：待实施。

### 6.13 任务 T13：P1 工作台壳与真实文件树接入

- 状态：待开始。
- 依赖：T3、T6～T12。
- 涉及文件/模块：`src/features/workbench/`、窗口标题、文件树、状态栏、目录决策。
- 目标：建立最终 P1 区域骨架并真实消费文件/窗口能力。
- 操作：先执行页面开发流程的定位、状态、提交边界和复用检查；接入标题、菜单、文件树、空/读/错状态、文件操作、目录决策；优先复用 T12 的同职责候选组件；消费 `DESIGN.md` token；实现 1050px 不溢出与 ≤820px 文件树抽屉/恢复入口；后续区域不展示假成功控件。
- 产出：可打开真实目录并管理 Markdown 文件的工作台壳。
- 影响范围：P1.1、P1.7 和后续页面容器。
- 边界与异常：不实现临时双编辑器、假页签或假大纲；阶段外可点击区域直接不渲染；抽屉关闭后焦点回触发点。
- 验证方式：工作区打开→文件操作→磁盘断言→窗口切换 E2E；完整桌面、1050px、820px 验证主区、滚动、抽屉、键盘和焦点。
- 完成标准：P1 的第一阶段能力和原型区域关系一致，状态、路径、窗口标题同步。
- 实际落地情况：待实施。

### 6.14 任务 T14：第一阶段复用组件与状态契约

- 状态：待开始。
- 依赖：T12、T13。
- 涉及文件/模块：`src/components/`、组件清单、a11y helpers。
- 目标：避免 P1/P2 重复实现对话框、异步状态和路径状态行。
- 操作：执行页面开发流程的复用检查；对 P1/P2 已出现两次的同职责实现抽取或固化为 AppDialog、AsyncStatePanel、PathStatus、DesktopWindowStatus；统一 ready/loading/empty/error 与文件特有状态优先级；定义 focus trap、Esc、焦点恢复、aria-live、reduced-motion 和非颜色状态契约；全部消费 `DESIGN.md` token 并登记真实页面。
- 产出：复用组件、测试和组件清单。
- 影响范围：P1/P2 及后续页面。
- 边界与异常：不提前抽象三栏/页签/主题配置；只有第二次真实复用时进入公共层。
- 验证方式：组件单测、键盘遍历、无障碍树、P1/P2 集成测试。
- 完成标准：公共组件至少被两个真实场景消费，焦点和状态契约成立。
- 实际落地情况：待实施。

### 6.15 任务 T15：第一阶段自动化测试和故障注入

- 状态：待开始。
- 依赖：T2～T14。
- 涉及文件/模块：Rust tests、Vitest、Testing Library、WDIO、`tests/fixtures/workspaces/`。
- 目标：形成可复现的文件、页面、窗口和异常测试基线。
- 操作：建立临时工作区工厂；测试路径越界、根为符号链接、链接越界/循环、权限、重名、回收站降级、外部变化、安全写及临时残件清理失败、状态仓储损坏、窗口去重；页面覆盖 P1/P2 状态、关键宽度、键盘和焦点；macOS 必须从正式 P2 入口真实触发 `select_workspace_folder`/`select_markdown_file`，观察原生面板、过滤、取消零副作用、IPC 返回、父目录确认与 `core:default` capability 下的命令可用性，不能以直接注入路径的单测代替。
- 产出：单元、集成、组件和桌面 E2E 套件。
- 影响范围：第一阶段验收和后续回归。
- 边界与异常：测试不得触碰用户真实目录；系统 UI 不稳定项与命令层分离。
- 验证方式：`pnpm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`pnpm test:e2e`。
- 完成标准：关键错误分支有断言，测试夹具清理完整，无依赖本机固定路径；macOS 真实选择器端到端证据齐全，否则 T15 与第一阶段不得标记完成。
- 实际落地情况：待实施。

### 6.16 任务 T16：macOS/Windows CI 门禁与构建产物

- 状态：待开始。
- 依赖：T1、T15。
- 涉及文件/模块：`.github/workflows/ci.yml`。
- 目标：在没有本地 Windows 设备的情况下取得真实 Windows runner 证据。
- 操作：配置 `macos-latest`/`windows-latest` 矩阵；Node 24.11.1、pnpm 精确版本、已锁定 Rust 工具链和缓存；硬门禁运行格式、lint、类型、前端测试、Rust 测试和 Tauri build；Windows 必须实际编译 T4 `MoveFileExW` 原子替换分支并覆盖目标存在/占用/替换失败，实际编译 T5 Dialog 插件与命令，并验证原生目录/文件面板的过滤、取消、IPC 返回及运行时 capability；执行可重复的 WDIO Tauri E2E；上传未签名测试构建、日志、截图和报告。单个 E2E 失败最多自动重试一次，重复波动用例必须登记原因并隔离，不能靠无限重试或静默放行核心链路。
- 产出：双平台 CI 和可下载测试产物。
- 影响范围：所有提交的阶段门禁。
- 边界与异常：CI 通过不等同 Windows 人工 UX 验收；无签名产物仅用于测试，不作为正式发布。
- 验证方式：GitHub Actions 运行链接、矩阵日志、E2E 报告和 artifact 哈希。
- 完成标准：两个平台均编译和测试通过；失败阻止阶段完成。
- 实际落地情况：待实施；需分支推送后获取远端运行结果。

### 6.17 任务 T17：第一阶段验收、证据和交接

- 状态：待开始。
- 依赖：T15、T16。
- 涉及文件/模块：`agent-works/markdown-editor-desktop/stage-1-desktop-foundation/` 下测试记录和计划状态更新。
- 目标：只基于真实结果更新 R1、R2、R5、R8、R11、R14、R30、R31 的第一阶段覆盖状态。
- 操作：macOS 实机走查；汇总 Windows CI；记录未执行的 Windows 原生 UI 人工项；核对页面/原型、配置和回滚；同步本计划实际落地情况。
- 产出：阶段 1 验收记录、缺口和下一独立阶段的输入。
- 影响范围：是否允许进入第二阶段。
- 边界与异常：所有子集 R 项保持部分覆盖，直至后续独立阶段完成；不得用底层测试替代产品整体验收。
- 验证方式：逐项证据链接、命令输出、截图/录屏和失败清单。
- 完成标准：阶段 1 里程碑成立，无未解释的超范围或虚假完成状态。
- 实际落地情况：待实施。

## 7. 数据库、配置、菜单与初始化计划

### 7.1 数据库与 SQL

本次不涉及数据库或 SQL 变更，不创建 SQL 文件。全文搜索阶段必须在其独立计划中重新评估 SQLite/FTS5；在此之前 Markdown 文件和版本化 Rust JSON 状态仓储足以承载第一阶段数据。

### 7.2 配置与初始化路径

| 路径/数据 | 用途 | 执行时机 | 影响范围 | 回滚方式 |
| --- | --- | --- | --- | --- |
| `.nvmrc` | 固定 Node 24.11.1 | 本地进入项目时 `nvm use` | 开发工具链 | 删除后回到个人默认，但 CI 仍固定 24.11.1；不建议 |
| `package.json` / `pnpm-lock.yaml` | 前端依赖、脚本、精确版本 | 安装、构建、测试 | 前端与 Tauri CLI | 回退提交并重新 `pnpm install --frozen-lockfile` |
| `rust-toolchain.toml` / `Cargo.lock` | Rust stable、rustfmt、clippy 和 Rust 依赖 | Cargo 构建 | Rust 后端 | 回退锁文件和工具链配置 |
| `src-tauri/tauri.conf.json` | identifier、窗口、构建、CSP、bundle | 开发启动和构建 | 桌面壳 | 回退配置；构建产物重新生成 |
| `src-tauri/capabilities/*.json` | Tauri 最小权限 | 窗口创建时 | IPC/插件权限 | 回退能力文件；不得扩大到全 HOME |
| `src-tauri/Cargo.toml` / `Cargo.lock` 中的 `tauri-plugin-dialog`、`sha2`、`getrandom` | Rust 后端系统选择器、稳定工作区 ID 与不可预测选择令牌 | 应用启动注册插件；用户触发选择命令时打开系统面板 | 本机选择器与授权提案，不提供前端路径权限 | 回退 T5 代码与依赖；不影响 Markdown 或已有状态文件 |
| `src-tauri/src/state.rs` 中的 schema + `appDataDir()/plainroot-state-v1.json` | 最近工作区、根窗口会话；不保存打开方式 | 应用 setup 首次初始化，最近记录或根会话变化后由 Rust 仓储原子更新 | 本机辅助状态；上限 8 MiB，Unix 新文件权限 `0600` | 退出应用后备份并删除即可在下次启动回到空最近记录/空根会话；不影响 Markdown |
| Tauri window-state 插件在 `appDataDir()` 下的运行时存储（准确文件名由 T1 PoC 登记） | 原生窗口位置、尺寸和最大化状态 | 窗口移动/缩放后由插件更新，创建窗口时读取 | 当前本机窗口外观，不含页签/文档内容 | 退出应用后备份并删除该插件状态即可回到系统默认窗口；不影响根会话和 Markdown |
| `.github/workflows/ci.yml` | macOS/Windows 构建测试矩阵 | push/PR/workflow_dispatch | 远端验证 | 回退工作流提交，不影响本地数据 |

### 7.3 菜单和权限注册

- 菜单定义路径：`src-tauri/src/menu.rs`；应用 setup 时注册，窗口创建后同步启用状态。
- 第一阶段启用：打开文件夹、打开 Markdown 文件、新建窗口、关闭窗口、在文件管理器中显示、基础帮助/关于。
- 第一阶段禁用但保留顶层位置：编辑、显示及后续文档命令；必须呈现禁用状态，不绑定无行为成功反馈。
- P2 原型的“设置”图标第一阶段隐藏，不进入菜单或页面；打开方式偏好只能在后续真实设置页具备“恢复为每次询问”入口时一起纳入。
- 权限路径：`src-tauri/capabilities/`。T5 虽注册 Dialog 插件，但只从 Rust 后端调用，`default.json` 仍仅有 `core:default`，不授予前端 Dialog、Store 或文件系统权限；后续插件也只能在真实消费者接入时增加最小 capability。
- 工作区文件权限由 Rust `WorkspaceRegistry` 在运行时校验，不给前端通用文件系统插件配置 `$HOME/**/*`。
- 影响范围：仅当前本机应用进程和用户主动授权的根目录。
- 回滚：移除对应 capability/plugin 并回退命令注册；已写 Markdown 不随配置回滚，本地状态文件可在退出应用后独立备份/删除。

## 8. 测试与验证计划

### 8.1 单元测试

- 计划：Rust 覆盖路径规范化、根内判定、文件名校验、文件修订、安全写入、状态 schema 迁移和窗口去重；TypeScript 覆盖 view model、过滤、状态 reducer 和组件契约。
- 具体完成情况：T1～T7 共 66 个 Rust 单测和 9 个前端状态测试已通过；T7 新增默认扩展名、双平台非法/保留名、并发重名、权限拒绝、路径越界、无覆盖及大小写重命名、移动、目录后代移动拒绝、跨设备错误映射、创建同步失败清理、变更枚举 parity，以及前端成功后提交、失败保持、跨父目录重映射和过期 mutationId 拒绝。安全写入和完整窗口协调的后续用例仍待对应任务。

### 8.2 接口与集成测试

- 计划：用临时工作区调用 Rust 命令，覆盖扫描、读、新建、重命名、移动、删除、监听、状态持久化和根窗口恢复；不触碰用户目录。
- 具体完成情况：T4 已验证状态持久化；T5 已用临时目录验证选择解析、授权登记、最近记录提交与复检；T6 已验证后台扫描服务、批次/取消状态、读取结果与前端 transport/state 的生产编译。真实系统选择器 UI、Tauri IPC、P1 文件树交互和根窗口恢复仍待 T12/T15 及对应任务。

### 8.3 页面/交互测试

- 计划：P2 覆盖选择取消、父目录授权展示、加载、最近过滤、可点击清除、失效、重新授权后继续、移除和部分恢复，并断言设置按钮不渲染；P1 覆盖文件树 CRUD、磁盘提交后刷新、错误回退、每次三选目录决策和同目录聚焦；覆盖完整桌面、1050px、820px 与 P2 760px。
- 具体完成情况：待验证。

### 8.4 组件资产闭环测试

- 计划：验证 AppDialog、AsyncStatePanel、PathStatus 等被 P1/P2 真实消费；检查状态优先级、焦点圈定、Esc、焦点恢复、aria-live、reduced-motion 和非颜色状态；扫描页面只消费 `DESIGN.md` 语义 token，组件清单与代码一致。
- 具体完成情况：待验证。

### 8.5 回归测试

- 计划：把 T15 的路径安全、扫描/CRUD、安全写、状态仓储、窗口去重、P1/P2 ready/loading/empty/error、键盘/焦点和关键宽度用例作为后续阶段不可删除的第一阶段回归基线；任何后续编辑、页签、搜索或主题改动都必须先运行受影响基线。对修复过的缺陷新增最小复现用例，不以改快照掩盖行为回归。
- 具体完成情况：待验证；T17 交接时登记基线命令、用例清单、运行时长和已知不稳定项。

### 8.6 异常与边界测试

- 计划：覆盖 `..`、绝对路径注入、根本身为符号链接、根内链接越界/循环、只读目录、权限撤销、路径失效、非 UTF-8、千文件扫描、事件风暴、重名/平台保留名、回收站不可用、写入各阶段中断、临时残件清理失败与启动重试、状态损坏/未知版本/不可写、窗口创建失败和最近记录不删除磁盘。
- 具体完成情况：T4 已验证状态异常；T5 已验证选择取消、格式不支持、文件链接、父目录未确认、令牌不存在/过期/淘汰、链接换靶、最近路径失效和重复目录；其他文件/页面异常仍待对应任务。UI 错误承接尚未进入 T12。

### 8.7 跨平台与 CI

- 计划：
  - macOS 本机：构建、真实窗口、系统选择器、Finder 定位、废纸篓和多窗口冒烟；
  - Windows GitHub runner：Rust/前端测试、Tauri 构建、稳定 WDIO 桌面 E2E 和测试产物；
  - Windows 人工：系统选择器、回收站、Explorer 定位、窗口菜单和安装包交互登记为最终跨平台阶段输入。
- 波动预案：编译、单元、集成和核心命令链路始终是硬门禁；单个 WDIO 用例最多重试一次并上传日志/截图。重复波动用例必须登记原因、隔离并保留人工清单，不能通过增加重试次数把失败写成通过。
- 具体完成情况：待验证；远端 CI 需分支推送后执行，未取得的 Windows 人工证据保持“未验证”。

### 8.8 建议执行命令

```bash
nvm use
corepack pnpm install --frozen-lockfile
pnpm lint
pnpm typecheck
pnpm test
pnpm build
cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml
pnpm tauri build --no-bundle
pnpm test:e2e
```

- 图示与原型一致性验证：P1/P2 实现必须覆盖本阶段涉及的原型区域、入口、关键状态和响应式退化；后续区域不能用假数据/假 toast 伪装完成。
- 配置闭环验证：T4 状态仓储的默认值、保存、读取、生效、损坏回退、不可写状态和版本兼容均有测试。
- 需求映射验证：R1～R34 均能在第 3、5 章找到第一阶段真实任务或逐项跳过原因；不存在阶段外占位任务。
- 页面功能点验证：7.1.1～7.3.3 均在第 4 章映射到任务、状态、权限、异常和验证方式。

## 9. 发布与回滚

- 发布前检查：第一阶段仅产出内部测试构建，不对外发布；必须通过 T15/T16，记录 Windows 人工未验收项。
- 发布步骤：CI 生成 macOS/Windows 未签名测试产物并附哈希；正式签名、notarization、MSI/NSIS 选择和更新通道必须在独立发布/跨平台收口计划中确认。
- 配置或环境变量：第一阶段无业务密钥；GitHub Actions 使用标准 runner，不提交 token；代码签名秘密不在本阶段配置。
- 代码回滚：回退阶段提交并重新生成构建；状态 schema v1 保持向后兼容，必要时退出应用、备份并删除状态文件以回到默认。
- 数据回滚：文件 CRUD 不提供应用级历史回滚；删除优先回收站；安全写失败保证原文件不被破坏。已成功修改的用户文件只能通过明确反向操作或系统恢复处理，不能随应用版本回滚静默改写。

## 10. 风险、假设与待确认项

### 10.1 风险

- Windows 原生选择器、回收站、Explorer 和菜单没有用户侧实机环境；CI 可覆盖真实 Windows 编译、命令和稳定 E2E，但不能完全替代人工可用性验收。缓解：T16 作为阶段门禁，T17 明确登记未验证人工项，最终跨平台阶段通过临时 Windows 设备、云桌面或受托测试取得证据。
- Windows WebView2 桌面 E2E 可能波动。缓解：构建/单元/命令集成为硬门禁；单用例最多重试一次并上传诊断；重复波动用例隔离并登记，核心链路不能静默放行。
- macOS 已安装并锁定 Rust 1.97.1，T1 构建链路已验证；Windows 工具链和同锁文件构建仍未验证，由 T16 建立 CI 后收口，当前不得把 macOS 结果外推为双平台通过。
- Windows 与 POSIX 的覆盖写语义不同；T10 必须做平台适配和故障注入，不能只用通用 rename 假定原子性。
- 文件监听在不同平台产生的事件序列不同；T9 以最终磁盘一致为准并合并事件，不把单一事件序列写成跨平台契约。
- macOS App Store 沙箱的安全作用域书签会改变授权持久化方案。本阶段假设先做直接分发桌面应用；若确定 App Store 首发，需在发布计划前补充授权数据迁移设计。
- WebView 排版和系统字体存在平台差异；阶段 1 只验证壳和文件流程，R7/R32 视觉定稿由对应独立阶段计划承接。
- 单文件父目录可能很大或包含无关敏感文件。缓解：扫描前展示父目录授权范围和解析后的链接目标，允许取消；扫描渐进、可取消，且 Rust 只允许访问已确认 canonical root。
- 安全写临时文件可能在失败时遗留。缓解：即时清理、脱敏诊断、下次启动有界重试和残件数量测试，禁止无界累积。

### 10.2 已确认决策

- 使用用户已确认的 Tauri 2 + React + TypeScript 技术基线。
- 使用现有 NVM Node `v24.11.1`；`.nvmrc` 与 CI 固定该已验证补丁版本，`engines.node` 保留 Node 24 兼容范围。安全升级通过显式工具链变更完成，不在 CI 中静默漂移补丁版本。
- 单个 Markdown 文件的父目录作为工作区根，选中文件作为初始活动目标；该语义已随推荐方案获用户确认，不是本计划新增权限决策。第一阶段新增的范围展示与取消入口只是授权保护，不改变已确认语义。
- Windows 暂无用户侧实机，以 GitHub Actions `windows-latest` 取得编译、测试和稳定 E2E 证据，原生系统交互保持未验证直至后续取得人工证据。

### 10.3 非阻塞假设

- 第一阶段默认不跟随符号链接；显示/反馈但不跨越授权边界。
- 第一阶段 Rust JSON 状态仓储足够承载最近记录和根窗口会话；打开方式不持久化，SQLite/FTS5 由全文搜索独立计划再评估。
- R2 的编辑会话依赖项、R5 的完整状态机、R14 的全部页签保存检查按用户确认延后，不改变这些 R 项最终验收含义。

### 10.4 待确认项

- 无阻塞计划启动的问题。
- 非阻塞：正式应用 bundle identifier、图标和签名主体在首次可分发构建前确认；阶段 1 暂使用可替换的开发标识，但必须集中配置，不能散落硬编码。
- 非阻塞：Windows 人工验收的具体渠道可在最终跨平台阶段选择临时 Windows 设备、云桌面或受托测试者；在取得证据前 R1 不标记最终已完成。

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum core capability for the Plainroot main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
